# Invader Game

This repository contains a simple Space Invaders style game using HTML5 Canvas.

## Setup

Install dependencies using pip:

```bash
pip install -r requirements.txt
```

The requirements file currently lists `pygame`, which may be used in future enhancements or for running the game in a desktop environment.

## Running

Open `index.html` in a modern web browser to play.

